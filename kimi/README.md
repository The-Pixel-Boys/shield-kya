# Kimi Code CLI + Shield KYA

Auto-wire: `kya connect kimi` (global `~/.kimi-code/mcp.json`) or `kya connect kimi --project` (`.kimi-code/mcp.json`).

Manual: merge `mcp.example.json` into your mcp.json (classic `mcpServers` shape).

Verify from the `/mcp-config` panel — shield-kya should show three tools.

Full recipe: [docs/hosts/kimi.md](../docs/hosts/kimi.md)
